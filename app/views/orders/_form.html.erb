<%= form_for(order) do |f| %>
<% if order.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

  <ul>
    <% order.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

<div class="field">
  <%= f.label :amount_of_item %>
  <%= f.number_field :amount_of_item %>
</div>

<div class="field">
  <%= f.label :cost %>
  <%= f.text_field :cost %>
</div>

<div class="field">
  <%= f.label :user_id %>
  <%= f.hidden_field :user_id, :value => current_user.id %>
</div>

<div class="actions">
  <%= f.submit %>
</div>
<% end %>


<%= form_tag("/orders", method: "post") do %>
<div class="field">
  <%= label_tag "ITEM" %>
  <%= select_tag 'name', options_for_select(@items.collect{ |u| [u.name] }) %>
</div>

<div class="field">
<%= label_tag "COST PER ITEM" %>
<%= select_tag 'cost', options_for_select(@items.collect{ |u| [u.cost] }) %>
</div>

<div class="actions">  
<%= submit_tag("Create") %>
</div>
<% end %>



